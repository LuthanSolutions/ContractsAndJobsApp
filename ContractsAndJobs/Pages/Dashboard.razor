@page "/dashboard"
<div>
    <SfDashboardLayout CellSpacing="@(new double[]{10 ,10 })" Columns="4">
        <DashboardLayoutPanels>
            <DashboardLayoutPanel>
                <HeaderTemplate><div>Panel 0</div></HeaderTemplate>
                <ContentTemplate>
                    <SfSparkline DataSource="PopulationData" TValue="PopulationReport" XName="Year" YName="Population" Type="SparklineType.Pie">
                    </SfSparkline>
                    <SfButton OnClick="@this.OnButtonClicked">Click</SfButton>
                </ContentTemplate>
            </DashboardLayoutPanel>

            <DashboardLayoutPanel SizeX=2 SizeY=2 Column=1>
                <HeaderTemplate><div>Panel 1</div></HeaderTemplate>
                <ContentTemplate>
                    <SfSparkline DataSource="PopulationData" TValue="PopulationReport" XName="Year" YName="Population" Type="SparklineType.Column">
                    </SfSparkline>
                </ContentTemplate>
            </DashboardLayoutPanel>

            <DashboardLayoutPanel SizeY=2 Column=3>
                <HeaderTemplate><div>Panel 2</div></HeaderTemplate>
                <ContentTemplate>
                    <SfCard ID="fixed-height-card">
                        <CardHeader Title="Glen Wilkin" SubTitle="Consultant Principal Developer" ImageUrl="/images/glen.jpg">
                        </CardHeader>
                        <CardContent>
                            @cardContent
                        </CardContent>
                        <CardFooter>
                            This is the card footer
                        </CardFooter>
                    </SfCard>
                </ContentTemplate>
            </DashboardLayoutPanel>

            <DashboardLayoutPanel Row=1>
                <HeaderTemplate><div>Panel 3</div></HeaderTemplate>
                <ContentTemplate>
                    <SfCard>
                        <CardHeader Title="Debunking Five Data Science Myths" SubTitle="By John Doe | Jan 20, 2018" />
                        <CardContent Content="Tech evangelists are currently pounding their pulpits about all things AI, machine learning, analytics—anything that sounds like the future and probably involves lots of numbers. Many of these topics can be grouped under the intimidating term data science." />
                        <CardFooter>
                            <SfButton OnClick="@this.OnButtonClicked">Click</SfButton>
                        </CardFooter>
                    </SfCard>
                </ContentTemplate>
            </DashboardLayoutPanel>
        </DashboardLayoutPanels>
    </SfDashboardLayout>
</div>

